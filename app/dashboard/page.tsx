const Dashboard = () => {
  // Check if user is authenticated
  // check if account exists ? redirect to right page or create the account in the database
  return <div>Dashboard</div>;
};
export default Dashboard;
